# Server Configuration
PORT=4000
NODE_ENV=development

# AI LLM Provider Configuration (Keep blank for now, uses mock provider)
LLM_PROVIDER=mock
OPENAI_API_KEY=
GEMINI_API_KEY=
ANTHROPIC_API_KEY=

# TTS Provider Configuration (Keep blank for now, uses mock provider)
TTS_PROVIDER=mock
ELEVENLABS_API_KEY=
OPENAI_TTS_API_KEY=

# Image Generation Provider Configuration (Keep blank for now, uses mock provider)
IMAGE_PROVIDER=mock
REPLICATE_API_KEY=
STABILITY_API_KEY=
